我们要做的事情很简单：在 main 函数里有一个空指针 my_p，我们想通过调用一个函数，给 指针my_kaap 分配一块内存，并存入一个数字 100。

**抓住关键**: 
我们要给一级指针变量分配一块内存，其实就是要修改一个**一级指针变量的值(让其指向一个新的地址)**
既然我们要改变一级指针变量的值，就应该传其地址，传的一个地址的地址，所以要用二级指针来接收

`int *my_p = NULL;`